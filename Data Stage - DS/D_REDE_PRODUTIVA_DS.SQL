CREATE TABLE D_REDE_PRODUTIVA_DS
(
    ID_REDE_PRODUTIVA   NUMBER        NOT NULL,
    REDE_PRODUTIVA_DESC VARCHAR2(100) NOT NULL,
    LATITUDE            NUMBER(23, 15),
    LONGITUDE           NUMBER(23, 15),
    TIPO_REDE_ID        NUMBER        NOT NULL,
    TIPO_REDE_DESC      VARCHAR2(50),
    FG_STATUS_ETL       CHAR(1),
    DT_INCLUSAO_DS      DATE DEFAULT SYSDATE,

    CONSTRAINT PK_D_R_PRODUTIVA_DS PRIMARY KEY (ID_REDE_PRODUTIVA)
);

CREATE INDEX IDX_D_REDE_PRODUTIVA ON D_REDE_PRODUTIVA_DS (ID_REDE_PRODUTIVA, FG_STATUS_ETL) TABLESPACE TBP_BI_DS_USER_IDX;