CREATE TABLE D_COOPERATIVA_DS
(
    ID_COOPERATIVA        NUMBER        NOT NULL,
    COOPERATIVA_NOME      VARCHAR2(100) NOT NULL,
    ENDERECO              VARCHAR2(500) NOT NULL,
    CIDADE                VARCHAR2(100) NOT NULL,
    LATITUDE              NUMBER(23, 15),
    LONGITUDE             NUMBER(23, 15),
    SIGLA                 VARCHAR2(20),
    AREA_ATUACAO_ID       NUMBER        NOT NULL,
    AREA_ATUACAO_DESC     VARCHAR2(150) NOT NULL,
    LOCALIZACAO_ID        NUMBER        NOT NULL,
    LOCALIZACAO_DESC      VARCHAR2(500) NOT NULL,
    TIPO_ORGANIZACAO_ID   VARCHAR2(100) NOT NULL,
    TIPO_ORGANIZACAO_NOME VARCHAR2(100) NOT NULL,
    ANO_INICIO            NUMBER        NOT NULL,
    FG_STATUS_ETL         CHAR(1) DEFAULT 'E',
    DT_INCLUSAO_DS        DATE    DEFAULT SYSDATE,

    CONSTRAINT PK_D_COOPERATIVA_DS PRIMARY KEY (ID_COOPERATIVA)
);

CREATE INDEX IDX_D_COOPERATIVA ON D_COOPERATIVA_DS (ID_COOPERATIVA, FG_STATUS_ETL) TABLESPACE TBP_BI_DS_USER_IDX;