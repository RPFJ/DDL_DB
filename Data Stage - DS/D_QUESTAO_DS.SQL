CREATE TABLE D_QUESTAO_DS
(
    ID_QUESTAO          NUMBER        NOT NULL,
    ID_RESPOSTA         NUMBER NOT NULL, 
    DESC_QUESTAO        VARCHAR(500) NOT NULL, 
    DESC_RESPOSTA         VARCHAR2(500) NOT NULL,
    FG_STATUS_ETL       CHAR(1),
    DT_INCLUSAO_DS      DATE DEFAULT SYSDATE,

    CONSTRAINT PK_D_QUESTAO_DS PRIMARY KEY (ID_QUESTAO, ID_RESPOSTA)
);

CREATE INDEX IDX_D_QUESTAO ON D_QUESTAO_DS (ID_QUESTAO, ID_RESPOSTA, FG_STATUS_ETL) TABLESPACE TBP_BI_DS_USER_IDX;